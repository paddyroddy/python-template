[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools",
    "setuptools-scm",
]

[project]
authors = [
    {email = "test@gmail.com", name = ""},
]
classifiers = [
    "License :: OSI Approved :: BSD License",
    "Programming Language :: Python :: 3 :: Only",
    "Typing :: Typed",
]
dependencies = []
description = ""
dynamic = [
    "version",
]
keywords = []
name = "example"
readme = "README.md"
requires-python = ">=3.8"
license.text = "BSD-3-Clause"
optional-dependencies.dev = [
    "black",
    "mypy",
    "pre-commit",
    "tox",
]
urls.homepage = "https://github.com/paddyroddy/python-template"

[tool.coverage]
report = {skip_covered = true, sort = "cover"}
run = {branch = true, parallel = true, source = ["example"]}
paths.source = [
    "src",
    ".tox*/*/lib/python*/site-packages",
]

[tool.mypy]
explicit_package_bases = true
plugins = [
    "numpy.typing.mypy_plugin",
    "pydantic.mypy",
]

[tool.pytest.ini_options]
addopts = "--color=yes -v"
testpaths = [
    "tests",
]

[tool.ruff]
fix = true
force-exclude = true
select = [
    "A",
    "B",
    "C",
    "DTZ",
    "E",
    "EM",
    "F",
    "FBT",
    "I",
    "ICN",
    "ISC",
    "N",
    "PLC",
    "PLE",
    "PLR",
    "PLW",
    "Q",
    "RUF",
    "S",
    "SIM",
    "T",
    "TID",
    "UP",
    "W",
    "YTT",
]
isort.known-first-party = ["example"]
mccabe.max-complexity = 18
pep8-naming.classmethod-decorators = ["classmethod", "pydantic.validator"]

[tool.setuptools_scm]

[tool.tomlsort]
sort_inline_arrays = false
spaces_indent_inline_array = 4
trailing_comma_inline_array = true

[tool.tox]
legacy_tox_ini = """
    [gh-actions]
    python =
        3.10: py310

    [testenv]
    commands =
        pytest --cov --slow
    deps =
        pytest
        pytest-cov
        pytest-skip-slow

    [tox]
    env_list =
        py310
"""
