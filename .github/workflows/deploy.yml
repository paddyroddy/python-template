---
name: Deploy

# yamllint disable-line rule:truthy
on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # yamllint disable-line rule:line-length
      - uses: paddyroddy/.github/actions/python/deployment@b45ffef611d12e0263916cf6f650a9b2cc13aa4f # v0
        with:
          pyproject-toml: ./pyproject.toml
          python-version: "3.12"
