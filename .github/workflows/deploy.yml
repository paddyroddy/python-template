---
name: Deploy

on:
  push:
    tags:
      - v*

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # yamllint disable-line rule:line-length
      - uses: paddyroddy/.github/actions/python/deployment@c3bb562d91f4004231cdd8497106a209a7531f40 # v0
        with:
          pyproject-toml: ./pyproject.toml
          python-version: "3.12"
